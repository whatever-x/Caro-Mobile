# AGENTS.md

이 저장소에서 Codex의 모델 선택 기준은 **하위 에이전트를 활용할 때만** 적용합니다. 여기에는 `spawn_agent` 등의 직접 위임과 Superpowers의 `subagent-driven-development` 스킬을 통한 위임이 포함됩니다. 메인 에이전트의 모델·추론 수준은 이 문서에서 선택하거나 변경하지 않습니다. Codex가 아닌 에이전트를 사용하거나 모델 선택 이외의 지침이 필요한 경우에는 [`CLAUDE.md`](./CLAUDE.md)를 참조합니다.

## 하위 에이전트 모델 선택 규정

Codex가 하위 에이전트에게 작업을 위임할 때 하위 작업의 성격에 따라 다음 모델과 추론 수준을 사용합니다.

| 작업 | 권장 모델 / 추론 수준 | 판단 |
|---|---|---|
| PRD·제품 설계 | Sol / high | 적절 |
| 기술 설계·아키텍처 | Sol / high~xhigh | 영향 범위가 크면 xhigh |
| 단순 자료 탐색 | Terra / low | 적절 |
| 비교·검증이 필요한 조사 | Terra / medium | low로는 누락 가능 |
| 일반 분석 | Terra / medium | 적절 |
| 일반 코드 구현 | Terra / medium | 적절 |
| 복잡하거나 위험한 구현 | Sol / high | 적절 |
| 단순·반복 작업 | Luna / low | 적절 |
| 일반 코드 리뷰 | Terra / medium~high | 매번 Sol은 과할 수 있음 |
| 배포 전·핵심 변경 최종 리뷰 | Sol / xhigh | 적절 |

- 하위 에이전트를 생성할 때 위임할 작업을 가장 가까운 유형으로 분류해 모델과 추론 수준을 선택합니다.
- 경계가 모호하거나 영향 범위·위험도가 큰 경우 더 높은 추론 수준을 선택합니다.
- 여러 유형이 섞인 경우 하위 작업별로 분류하되, 한 하위 에이전트 안에서 분리하기 어려우면 가장 위험한 단계의 기준을 적용합니다.
- 사용자가 하위 에이전트의 모델 또는 추론 수준을 명시하면 사용자의 지시를 우선합니다.
- 현재 환경에서 해당 모델이나 추론 수준을 선택할 수 없으면 사용 가능한 가장 가까운 설정으로 진행합니다.

## 적용 범위 및 토큰 효율

- 위 표는 `spawn_agent` 등 하위 에이전트 생성과 Superpowers의 `subagent-driven-development` 스킬로 하위 에이전트에게 작업을 위임할 때만 적용합니다. 메인 에이전트, 새 Codex 작업, 자동화의 모델 선택에는 적용하지 않습니다.
- `subagent-driven-development` 스킬을 사용할 때도 각 하위 작업을 위 표에 따라 분류하고, 해당 모델·추론 수준 기준으로 개발을 진행합니다.
- 메인 에이전트의 실제 모델을 위 표와 비교하거나, 설정 차이를 이유로 새 작업을 만들거나 자동 이관하지 않습니다.
- 위 표는 하위 에이전트 생성을 허가하는 규칙이 아닙니다. 사용자나 적용 가능한 절차서가 하위 에이전트·위임·병렬 작업을 명시적으로 요구한 경우에만 하위 에이전트를 생성하며, 그 외에는 단일 에이전트를 기본값으로 사용합니다.
- 하위 에이전트를 사용할 때는 맡긴 작업과 선택한 모델·추론 수준을 사용자에게 알립니다. 현재 환경에서 실제 적용 여부를 확인할 수 없으면 적용됐다고 추측하지 않습니다.
- 하위 에이전트는 토큰 절약 수단으로 사용하지 않습니다. 서로 독립적인 작업이 둘 이상이고 병렬 처리, 전문화 또는 메인 컨텍스트 격리가 실질적으로 유리할 때만 사용합니다.
- 같은 품질을 달성할 수 있다면 가장 낮은 추론 수준을 사용합니다. Max·Ultra와 불필요한 다단계 위임은 복잡하고 분할 가능한 하위 작업에 한정합니다.

## 그 외 지침

Codex 하위 에이전트 전용 모델 선택 규정을 제외하면 `CLAUDE.md`를 단일 진실 공급원(single source of truth)으로 사용합니다. 프로젝트 개요·빌드 커맨드·아키텍처·컨벤션 플러그인·테스트·CI 등 모든 가이드라인은 해당 문서에 정리되어 있습니다.

작업 맥락(Compose/MVI/KMP/리뷰 등)에 맞는 절차서가 필요하면, `CLAUDE.md`의 **Skill(All Agents)** 섹션에 정리된 트리거-`SKILL.md` 매핑을 따릅니다. 위 하위 에이전트 모델 선택 규정 외의 Codex용 별도 규칙은 두지 않으며, 목록을 여기 중복하지 않고 `CLAUDE.md`와 `.claude/skills`를 기준으로 합니다.
